{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "shell": "powershell",
            "command": "$cmd = ($input | ConvertFrom-Json).tool_input.command; if ($cmd -match '^clasp push') { Write-Host 'clasp push 検出 — テストを実行します...'; cd 'C:\\Users\\1100717\\Documents\\exam_management'; node node_modules/jest-cli/bin/jest.js 2>&1 } else { exit 0 }",
            "timeout": 60,
            "statusMessage": "Jest テスト実行中..."
          }
        ]
      }
    ]
  }
}
